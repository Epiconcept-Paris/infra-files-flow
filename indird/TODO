x check config relations
x actions output to tmpfile
x handle rules

x implémentation partielle des specs de TDE à finir
x les paramètres doivent pas être en dur dans le script
x lancer actions selon RC
x log de suivi complet
x déplacement fichiers
x match regexp
x traitement séquentiel acceptable et même préférable
x commande de traitement ne doit pas bloquer (-> cmde prefix 'timeout')

x abandon :
    x retry en cas d'erreur.
    x pool de process boucle surveillant n répertoires (multidossier eSIS)
    x avec IPC un peu plus ellaboré que signal
    x python plutot que bash (pas assez souple): CGD dit pourquoi pas mais ds 2 ans

x doc technique
