---

- name: debug filename
  tags: ['subconf']
  debug:
    var: filename

- name: debug server
  tags: ['subconf']
  debug:
    var: server


# - name: create dirs in server
#   file:
#     status: directory
#     path: "/etc/indird.d/{{item | basename | splitext | first }}"
#   loop: "{{fichier_trouvés}}.files"
#   become: true
#   delegate_to: "{{server}}"


# - name: deploy conf files
#   copy:
#     src: "{{ tmpconffolder }}{{item.path | basename}}"